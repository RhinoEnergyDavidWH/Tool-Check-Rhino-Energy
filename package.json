{
  "name": "rhino-tool-check",
  "version": "1.0.0",
  "private": true,
  "description": "Rhino Tool Check — per-team tool checks for Rhino Energy Solutions, wrapped as an Android APK with Capacitor.",
  "scripts": {
    "cap:add:android": "cap add android",
    "cap:sync": "cap sync android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/filesystem": "^6.0.1",
    "@capacitor/share": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2"
  }
}
